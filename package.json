{
  "name": "webpack-clean-obsolete-chunks",
  "version": "0.1.4",
  "description": "A webpack plugin to remove obsolete chunk files in webpack watch mode",
  "author": "GProst a.k.a. German Prostakov <gprostmail@gmail.com>",
  "homepage": "https://github.com/GProst/webpack-clean-obsolete-chunks",
  "license": "MIT",
  "main": "index.js",
  "keywords": [
    "webpack",
    "clean",
    "remove",
    "delete",
    "old",
    "obsolete",
    "chunks",
    "plugin"
  ],
  "scripts": {
    "test-unit": "mocha \"test/unit/**/*.spec.js\"",
    "test-unit-watch": "mocha \"test/unit/**/*.spec.js\" --watch",
    "test-integration": "mocha \"test/integration/**/*.spec.js\" --timeout 15000",
    "coverage-unit": "istanbul cover _mocha -- \"test/unit/**/*.spec.js\"",
    "coverage-integration": "istanbul cover _mocha -- \"test/integration/**/*.spec.js\"",
    "travis-test": "mocha $SPECS_PATH --timeout 15000 && npm run coveralls",
    "coveralls": "istanbul cover _mocha --report lcovonly -- \"test/unit/**/*.spec.js\" && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/GProst/webpack-clean-obsolete-chunks"
  },
  "bugs": {
    "url": "https://github.com/GProst/webpack-clean-obsolete-chunks/issues"
  },
  "dependencies": {
    "del": "^2.2.2"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "coveralls": "^2.12.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "sinon": "^2.1.0"
  }
}
