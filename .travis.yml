language: node_js
node_js:
  - "7"
  - "6"
env:
  - SPECS_PATH="test/unit/**/*.spec.js"
  - SPECS_PATH="test/integration/**/*.spec.js"
cache:
  yarn: true

install:
  - yarn
  - cd $TRAVIS_BUILD_DIR/test/integration/env/webpack-1 && yarn
  - cd $TRAVIS_BUILD_DIR/test/integration/env/webpack-2 && yarn
  - cd $TRAVIS_BUILD_DIR
script:
  - yarn run travis-test

notifications:
  email:
    - gprostmail@gmail.com