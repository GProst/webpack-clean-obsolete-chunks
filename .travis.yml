language: node_js
node_js:
  - "7"
  - "6"
env:
  - SPECS_PATH="test/unit/**/*.spec.js"
  - SPECS_PATH="test/integration/**/*.spec.js"
cache:
  yarn: true

before_install: cd $TRAVIS_BUILD_DIR
install:
  - yarn
  - cd ./test/integration/env/webpack-1 && yarn
  - cd ./test/integration/env/webpack-2 && yarn
script:
  - yarn run travis-test

notifications:
  email:
    - gprostmail@gmail.com